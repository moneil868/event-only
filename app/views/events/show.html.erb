<h1>Event: <%= @event.name %> </h1>

<p>
  Date: <%= @event.date %> <br/>
  Capacity: <%= @event.capacity %> <br/>
  City: <%= @event.city %> <br/>
</p>

<%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: "Are you sure you want to delete the location '#{@event.name}'? This cannot be undone!"} %> | <%= link_to 'Edit', edit_event_path(@event) %> | <%= link_to('Back to Events', events_path) %>
